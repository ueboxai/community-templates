## 模板信息

- 模板 id：
- 引擎版本：
- 新增 / 更新：

## 自查

- [ ] 包里有 `.uproject`，没有 `Binaries` / `Intermediate` / `Saved` / `.git`
- [ ] 用 `python3 scripts/pack.py` 打包（或确认已排除上述目录）
- [ ] `manifest.json` 里的 `size` / `sha256` 与包一致
- [ ] 更新已有模板时提升了 `version`
- [ ] README「现有模板」表已更新
- [ ] 本地 `python3 scripts/validate.py` 通过
- [ ] 我有权以 `license` 字段声明的许可证发布这个模板
