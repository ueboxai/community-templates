# 安全策略

模板包解压出来是一个完整的 UE 工程，**打开时里面的 C++、插件、Python 脚本都会运行**。
所以这个仓库里的安全问题，影响的是每一个用模板新建工程的人。

## 怎么报告

**请不要在公开 issue 里描述可被利用的细节。** 通过 GitHub 私下报告：

<https://github.com/ueboxai/community-templates/security/advisories/new>

适合私下报告的问题：

- 某个模板包含恶意或可疑的代码、插件、脚本
- 能让校验脚本放过恶意模板包的漏洞（路径穿越、符号链接、zip 炸弹等）
- 清单签名、镜像同步等发布环节的问题

模板打不开、配置不对这类普通问题，直接[提 issue](https://github.com/ueboxai/community-templates/issues/new/choose)。

## 我们会怎么处理

1. 确认问题后，第一时间把对应条目从 `manifest.json` 里撤下并重新签名。
   客户端下次读取清单时就不会再列出这个模板
2. 已经用这个模板建出来的工程不会被自动处理，我们会在 advisory 里说明影响和处理办法
3. 修复后视情况重新上架，并在 advisory 里致谢报告者（如果你愿意）

## 这个仓库能保证什么、不能保证什么

- **能保证**：客户端下到的模板包，就是清单里写的那一份（sha256）；配置签名公钥之后，
  清单本身也是维护者签过名的那一份（minisign）
- **不能保证**：模板包是安全的。投稿会经过审核，CI 也会标出包里会被执行的内容，
  但审核不等于逐行审计。只打开你信任的作者的模板
